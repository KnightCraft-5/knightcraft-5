# Files packwiz should NOT index or ship in exports.
.git/
.gitignore
.gitattributes
.packwizignore
README.md

# World-specific — version-controlled but not distributed (defaultconfigs/ ships defaults instead).
serverconfig/

# Mod jars downloaded by packwiz-installer (dev setup = .minecraft) — ignore so
# refresh/export use the .pw.toml metadata, not the loose jars. Keep the two jars
# CurseForge won't redistribute via API.
mods/*.jar
!mods/Dungeon-Realm-1.20.1-1.1.7.jar
!mods/tl_skin_cape_forge_1.20_1.20.1-1.32.jar

# Guards against runtime data if the pack dir is ever used as a live instance.
logs/
crash-reports/
screenshots/
saves/
world/
backups/
journeymap/
local/
blueprints/
servers.dat
usercache.json
.sl_password
